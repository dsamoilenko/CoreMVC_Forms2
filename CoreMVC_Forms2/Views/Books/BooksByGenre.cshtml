@model IEnumerable<CoreMVC_Forms2.Models.Book>

@{
    ViewBag.Title = "Books by genre";
}

<h2>Books by genre</h2>

<table id="books" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Genre</th>
            <th>Pubdate</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@Html.ActionLink(book.Title, "Details", "Books", new { @id = book.Id }, null)</td>
                <td>@book.Author</td>
                <td>@book.Genre</td>
                <td>@book.PubDate</td>
                <td>@book.Price</td>
            </tr>
        }
    </tbody>
</table>